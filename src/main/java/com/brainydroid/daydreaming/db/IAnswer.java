package com.brainydroid.daydreaming.db;

public interface IAnswer {

    // Never used, but makes sure we won't forget to add a "type" member in
    // the implementations.
    public String getType();

}
