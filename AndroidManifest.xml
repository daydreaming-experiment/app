<?xml version="1.0" encoding="UTF-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.brainydroid.daydreaming"
    android:versionCode="12"
    android:versionName="0.4+next">

    <uses-sdk android:minSdkVersion="10" android:targetSdkVersion="16"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.VIBRATE"/>

    <application android:icon="@drawable/ic_launcher"
                 android:label="@string/app_name"
                 android:theme="@style/AppTheme"
                 android:name=".ui.App">

        <receiver android:name=".background.BootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>

        <receiver android:name=".background.NetworkReceiver">
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE"/>
            </intent-filter>
        </receiver>

        <receiver android:name=".background.TimeReceiver">
            <intent-filter>
                <action android:name="android.intent.action.TIMEZONE_CHANGED"/>
                <action android:name="android.intent.action.TIME_SET"/>
            </intent-filter>
        </receiver>

        <activity android:name=".ui.DashboardActivity"
                  android:label="@string/app_name" >

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity>

        <activity android:name=".ui.SettingsActivity"
                  android:label="@string/settings_title_activity">
        </activity>

        <activity android:name=".ui.QuestionActivity"
                  android:label="@string/app_name"
                  android:taskAffinity="">
        </activity>

        <activity android:name=".ui.FirstLaunchWelcomeActivity"
                  android:label="@string/firstLaunchWelcome_title_activity">
        </activity>

        <activity android:name=".ui.FirstLaunchDescriptionActivity"
                  android:label="@string/firstLaunchDescription_title_activity">
        </activity>

        <activity android:name=".ui.FirstLaunchProfileActivity"
                  android:label="@string/firstLaunchProfile_title_activity">
        </activity>

        <activity android:name=".ui.FirstLaunchTermsActivity"
                  android:label="@string/firstLaunchTerms_title_activity">
        </activity>

        <activity android:name=".ui.FirstLaunchPullActivity"
                  android:label="@string/firstLaunchPull_title_activity">
        </activity>

        <activity android:name=".ui.FirstLaunchMeasuresActivity"
                  android:label="@string/firstLaunchMeasures_title_activity">
        </activity>

        <service android:name=".background.LocationService"
                 android:exported="false"/>

        <service android:name=".background.SyncService"
                 android:exported="false"/>

        <service android:name=".background.PollService"
                 android:exported="false"/>

        <service android:name=".background.SchedulerService"
                 android:exported="false"/>

        <service android:name=".background.LocationPointService"
                 android:exported="false"/>

    </application>

</manifest>
